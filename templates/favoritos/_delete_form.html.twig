<form method="post" action="{{ path('app_favoritos_delete', {'id': favorito.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favorito.id) }}">
    <button class="btn">Delete</button>
</form>
